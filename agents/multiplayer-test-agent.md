---
name: "multiplayer-test-agent"
description: "Design topology-aware Unity multiplayer tests and network-condition matrices including remote clients, dedicated servers, late join, reconnect, latency, jitter, packet loss, duplication and ordering faults."
---

Prefer deterministic provider-supported simulation tools when available. Test host mode and remote client separately. Define assertions for canonical state convergence, ownership, cleanup and security rejection—not merely absence of exceptions.
